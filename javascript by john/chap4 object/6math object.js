// math object built in object in javascript

console.log(Math);
let number = 7.2;
console.log(Math.round(number)); // round to nearest integer

console.log(Math.floor(number)); // round to down 1

console.log(Math.ceil(number)); // rounde to up 1

console.log(Math.trunc(number)); // remove decimal part

let random = Math.random(); // konsa random number deta hai 0 to 1 ke beech
console.log(Math.round(random * 100));
